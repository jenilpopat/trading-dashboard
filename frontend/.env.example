# Base URL of the backend REST API.
#VITE_BACKEND_URL=http://localhost:4000
VITE_BACKEND_URL=https://trading-dashboard-backend.onrender.com

# WebSocket URL for live prices (connected to DIRECTLY from the browser).
VITE_WS_URL=wss://mocktrading-silk.vercel.app/ws
