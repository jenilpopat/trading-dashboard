# Port the backend HTTP server listens on
#PORT=4000

# Base URL of the upstream mock trading API (do not change unless it moves)
UPSTREAM_BASE_URL=https://mocktrading-silk.vercel.app

# Comma-separated list of origins allowed by CORS (the frontend dev server)
CORS_ORIGIN=https://trading-dashboard-1-lsxm.onrender.com
